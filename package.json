{
  "name": "ultimate-watermark-docs",
  "version": "1.0.0",
  "description": "Official documentation for the Ultimate Watermark WordPress plugin (Free + Pro)",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "vitepress build docs && node scripts/prepare-netlify.mjs",
    "docs:build": "vitepress build docs && node scripts/prepare-netlify.mjs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
